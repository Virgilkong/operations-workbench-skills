# 运营工作台 · Skill 共享库

这是公开的 Skill 内容仓库。运营工作台通过 Cloudflare Worker 读取和写入，上传后同事刷新原 HTML 即可查看。

- `catalog.json`：共享索引。
- `skills/`：Skill 正文与元数据。
- `activity/`：匿名点赞和评论。
- `favorites/`：浏览器匿名标识对应的点赞索引。

请仅分享可以公开的 Skill 文本，不上传业务报表、个人资料或密钥。计算引擎及业务数据不存放在此仓库。

维护者可通过 Git 历史审查和恢复内容。不要手工破坏索引与记录的一致性。
